/**
 * Deprecated
 *
 * Legacy classes kept for backward compatibility.
 *
 * These APIs are deprecated in the Tone.js-based
 * p5.sound architecture and should not be used in
 * new projects.
 */

declare namespace p5 {
  class MonoSynth {
    constructor(...args: any[]);
  }

  class PolySynth {
    constructor(...args: any[]);
  }

  class AudioVoice {
    constructor(...args: any[]);
  }

  class Effect {
    constructor(...args: any[]);
  }

  class Filter {
    constructor(...args: any[]);
  }

  class EQ {
    constructor(...args: any[]);
  }

  class Compressor {
    constructor(...args: any[]);
  }

  class Distortion {
    constructor(...args: any[]);
  }

  class Convolver {
    constructor(...args: any[]);
  }

  class Pulse {
    constructor(...args: any[]);
  }

  class OnsetDetect {
    constructor(...args: any[]);
  }

  class Phrase {
    constructor(...args: any[]);
  }

  class Part {
    constructor(...args: any[]);
  }

  class Score {
    constructor(...args: any[]);
  }

  class SoundLoop {
    constructor(...args: any[]);
  }
}
